export default function Layout({children}) {
    return(
        <main>
            <nav>
                {/**Her skal nav importeres */}
            </nav>
            {children}
        </main>

    )
}